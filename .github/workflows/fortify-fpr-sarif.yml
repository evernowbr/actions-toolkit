name: Fortify FPR → SARIF Converter (Linux)

on:
  workflow_call:
    inputs:
      # Flag para ativar a conversão
      enable_sarif_multitool:
        description: 'Habilita conversão FPR→SARIF usando SARIF MultiTool'
        required: false
        default: 'true'
        type: string
      # Versão do .NET SDK
      dotnet_version:
        description: 'Versão do .NET SDK para instalar'
        required: false
        default: '8.0.x'
        type: string
      # Versão do SARIF MultiTool
      sarif_multitool_version:
        description: 'Versão do SARIF MultiTool'
        required: false
        default: '4.5.4'
        type: string
      # Nome do artefato FPR
      fpr_artifact_name:
        description: 'Nome do artefato contendo arquivos .fpr'
        required: false
        default: 'fortify-fpr'
        type: string
      # Validar SARIF gerado
      validate_sarif:
        description: 'Validar arquivos SARIF gerados'
        required: false
        default: 'true'
        type: string
      # Nome do projeto Fortify
      fortify_project_name:
        description: 'Nome do projeto Fortify'
        required: false
        default: 'Unknown'
        type: string
      # Versão do projeto Fortify
      fortify_version_name:
        description: 'Versão do projeto Fortify'
        required: false
        default: 'Unknown'
        type: string
    secrets:
      FORTIFY_SSC_API_URL:
        required: false
      FORTIFY_UNIFIED_LOGIN_TOKEN:
        required: false

jobs:
  convert-fpr-linux:
    name: Convert FPR to SARIF (Linux)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: ${{ fromJSON(inputs.enable_sarif_multitool) }}
    permissions:
      actions: read
      contents: read

    steps:
      # ========== CHECKOUT ==========
      - name: Checkout repository
        uses: actions/checkout@v4

      # ========== DOWNLOAD FPR FROM SSC ==========
      - name: Download FPR from SSC
        shell: bash
        env:
          FORTIFY_SSC_API_URL: ${{ secrets.FORTIFY_SSC_API_URL }}
          FORTIFY_UNIFIED_LOGIN_TOKEN: ${{ secrets.FORTIFY_UNIFIED_LOGIN_TOKEN }}
          FORTIFY_PROJECT_NAME: ${{ inputs.fortify_project_name }}
          FORTIFY_VERSION_NAME: ${{ inputs.fortify_version_name }}
        run: |
          echo "📥 Baixando .fpr do Fortify SSC..."
          
          # Configurações robustas
          set -euo pipefail
          IFS=$'\n\t'
          trap 'echo "❌ Script falhou na linha $LINENO"' ERR
          
          # Criar diretório para .fpr
          mkdir -p ./fpr-files
          
          # ========== FUNÇÕES UTILITÁRIAS LINUX ==========
          
          # Função para validação JSON
          validate_json_response() {
              local response="$1"
              local context="$2"
              
              if [ -z "$response" ]; then
                  echo "❌ Resposta vazia da API ($context)"
                  return 1
              fi
              
              if ! echo "$response" | jq -e . >/dev/null 2>&1; then
                  echo "❌ JSON inválido ($context):"
                  echo "$response" | head -10
                  return 1
              fi
              
              local error_msg=$(echo "$response" | jq -r '.message // empty')
              if [ -n "$error_msg" ]; then
                  echo "❌ Erro da API ($context): $error_msg"
                  return 1
              fi
              
              return 0
          }
          
          # Função para chamadas API
          api_call() {
              local method="${1:-GET}"
              local url="$2"
              local data="${3:-}"
              local context="$4"
              
              local curl_opts=(-sS -k -L --fail)
              curl_opts+=(-H "Content-Type: application/json")
              curl_opts+=(-H "Accept: application/json")
              curl_opts+=(-H "Authorization: FortifyToken $FORTIFY_UNIFIED_LOGIN_TOKEN")
              
              if [ "$method" = "POST" ] || [ "$method" = "PUT" ]; then
                  curl_opts+=(-X "$method")
                  if [ -n "$data" ]; then
                      curl_opts+=(-d "$data")
                  fi
              fi
              
              curl_opts+=("$url")
              
              local response
              local exit_code
              
              response=$(curl "${curl_opts[@]}" 2>/dev/null) || exit_code=$?
              
              if [ "${exit_code:-0}" -ne 0 ]; then
                  echo "❌ Falha na chamada API ($context): $exit_code"
                  return 1
              fi
              
              if ! validate_json_response "$response" "$context"; then
                  return 1
              fi
              
              echo "$response"
              return 0
          }
          
          # Função para buscar projeto/versão no Fortify SSC
          fortify_buscar_projeto_versao() {
              echo "🔍 Buscando projeto/versão no Fortify SSC..."
              
              if [ -n "$FORTIFY_SSC_API_URL" ] && [ -n "$FORTIFY_UNIFIED_LOGIN_TOKEN" ]; then
                  local project_response
                  local encoded_name=$(echo "$FORTIFY_PROJECT_NAME" | jq -rR '@uri')
                  project_response=$(api_call "GET" "$FORTIFY_SSC_API_URL/projectVersions?q=$encoded_name&fulltextsearch=true" "" "buscar projeto")
                  
                  if [ $? -eq 0 ]; then
                      local project_id=$(echo "$project_response" | jq -r '.data[] | select(.project.name=="'"$FORTIFY_PROJECT_NAME"'") | .project.id' | head -1)
                      local version_id=$(echo "$project_response" | jq -r '.data[] | select(.project.name=="'"$FORTIFY_PROJECT_NAME"'") | select(.name=="'"$FORTIFY_VERSION_NAME"'") | .id' | head -1)
                      
                      if [ -n "$version_id" ]; then
                          echo "✅ Projeto e versão encontrados: $version_id"
                          FORTIFY_PROJECT_VERSION_ID="$version_id"
                          return 0
                      elif [ -n "$project_id" ]; then
                          echo "✅ Projeto encontrado, versão não existe"
                          FORTIFY_PROJECT_ID="$project_id"
                          return 1
                      else
                          echo "⚠️ Projeto não encontrado"
                          return 2
                      fi
                  else
                      echo "⚠️ Falha ao buscar projeto no SSC"
                      return 3
                  fi
              else
                  echo "⚠️ Secrets do Fortify não configurados"
                  return 4
              fi
          }
          
          # Função para baixar .fpr do SSC
          download_fpr_from_ssc() {
              echo "📥 Baixando arquivo .fpr do SSC..."
              
              if [ -z "$FORTIFY_PROJECT_VERSION_ID" ]; then
                  echo "❌ ID da versão não encontrado"
                  return 1
              fi
              
              # Buscar artefatos da versão
              local artifacts_response=$(api_call "GET" "$FORTIFY_SSC_API_URL/projectVersions/$FORTIFY_PROJECT_VERSION_ID/artifacts" "" "buscar artefatos")
              
              if [ $? -eq 0 ]; then
                  # Encontrar arquivo .fpr
                  local fpr_file_id=$(echo "$artifacts_response" | jq -r '.data[] | select(.name | endswith(".fpr")) | .id' | head -1)
                  
                  if [ -n "$fpr_file_id" ] && [ "$fpr_file_id" != "null" ]; then
                      echo "✅ Arquivo .fpr encontrado: $fpr_file_id"
                      
                      # Baixar arquivo .fpr
                      local fpr_filename="$FORTIFY_PROJECT_NAME-$FORTIFY_VERSION_NAME.fpr"
                      local fpr_path="./fpr-files/$fpr_filename"
                      
                      echo "📥 Baixando: $fpr_filename"
                      
                      # Download do arquivo
                      curl -sS -k -L --fail \
                           -H "Authorization: FortifyToken $FORTIFY_UNIFIED_LOGIN_TOKEN" \
                           -H "Accept: application/octet-stream" \
                           "$FORTIFY_SSC_API_URL/projectVersions/$FORTIFY_PROJECT_VERSION_ID/artifacts/$fpr_file_id/content" \
                           -o "$fpr_path"
                      
                      if [ $? -eq 0 ] && [ -f "$fpr_path" ]; then
                          local file_size=$(wc -c < "$fpr_path")
                          echo "✅ Download concluído: $fpr_filename ($((file_size/1024))KB)"
                          return 0
                      else
                          echo "❌ Falha no download do arquivo .fpr"
                          return 1
                      fi
                  else
                      echo "⚠️ Nenhum arquivo .fpr encontrado para esta versão"
                      return 2
                  fi
              else
                  echo "❌ Falha ao buscar artefatos"
                  return 3
              fi
          }
          
          # Executar busca e download
          fortify_buscar_projeto_versao
          fortify_status=$?
          
          if [ $fortify_status -eq 0 ]; then
              download_fpr_from_ssc
              download_status=$?
              
              if [ $download_status -eq 0 ]; then
                  echo "✅ Download do .fpr concluído com sucesso"
              else
                  echo "❌ Falha no download do .fpr"
                  exit 1
              fi
          else
              echo "❌ Falha ao buscar projeto/versão no SSC"
              exit 1
          fi

      # ========== VALIDATE PREREQUISITES ==========
      - name: Validate prerequisites
        run: |
          echo "🔍 Validando pré-requisitos..."
          
          # Sanitização mais robusta de inputs
          if [[ ! "${{ inputs.fortify_project_name }}" =~ ^[a-zA-Z0-9_.-]{1,50}$ ]]; then
            echo "❌ Nome do projeto inválido (deve ter 1-50 chars: a-zA-Z0-9_.-)"
            exit 1
          fi
          
          if [[ ! "${{ inputs.fortify_version_name }}" =~ ^[a-zA-Z0-9_.-]{1,30}$ ]]; then
            echo "❌ Nome da versão inválido (deve ter 1-30 chars: a-zA-Z0-9_.-)"
            exit 1
          fi
          
          # Validar versão do SARIF MultiTool
          if [[ ! "${{ inputs.sarif_multitool_version }}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "❌ Versão SARIF MultiTool inválida (formato: x.y.z)"
            exit 1
          fi
          
          # Mascarar tokens com validação
          if [[ -n "${{ secrets.FORTIFY_UNIFIED_LOGIN_TOKEN }}" ]]; then
            echo "::add-mask::${{ secrets.FORTIFY_UNIFIED_LOGIN_TOKEN }}"
          fi
          
          if [[ -n "${{ secrets.FORTIFY_SSC_API_URL }}" ]]; then
            echo "::add-mask::${{ secrets.FORTIFY_SSC_API_URL }}"
          fi
          
          # Verificar se FPR artifact existe
          if [ ! -d "./fpr-files" ]; then
            echo "❌ Diretório fpr-files não encontrado"
            exit 1
          fi
          
          # Verificar se há arquivos FPR
          fpr_files=$(find ./fpr-files -name "*.fpr" 2>/dev/null | wc -l)
          if [ "$fpr_files" -eq 0 ]; then
            echo "❌ Nenhum arquivo .fpr encontrado"
            exit 1
          fi
          
          echo "✅ Encontrados $fpr_files arquivos FPR"
          
          # Verificar tamanho dos arquivos
          find ./fpr-files -name "*.fpr" -size +100M | while read large_file; do
            echo "⚠️ Arquivo grande detectado: $large_file"
          done

      # ========== SETUP .NET SDK ==========
      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ inputs.dotnet_version }}

      # ========== CACHE .NET TOOLS ==========
      - name: Cache .NET tools
        uses: actions/cache@v4
        with:
          path: ~/.dotnet/tools
          key: dotnet-tools-${{ inputs.sarif_multitool_version }}-${{ github.repository }}-${{ hashFiles('**/*.yml', '**/*.yaml') }}
          restore-keys: |
            dotnet-tools-${{ inputs.sarif_multitool_version }}-${{ github.repository }}-
            dotnet-tools-${{ inputs.sarif_multitool_version }}-

      # ========== INSTALL LINUX DEPENDENCIES ==========
      - name: Install Linux dependencies
        run: |
          echo "📦 Instalando dependências Linux..."
          sudo apt-get update
          sudo apt-get install -y jq curl
          echo "✅ Dependências Linux instaladas"

      # ========== INSTALL SARIF MULTITOOL ==========
      - name: Install SARIF MultiTool
        run: |
          echo "🔧 Instalando SARIF MultiTool v${{ inputs.sarif_multitool_version }}..."
          
          # Retry logic para instalação
          for attempt in {1..3}; do
            if dotnet tool install --global Sarif.Multitool --version ${{ inputs.sarif_multitool_version }}; then
              break
            else
              echo "⚠️ Tentativa $attempt falhou, tentando novamente em 5s..."
              sleep 5
            fi
          done
          
          # Adicionar ao PATH explicitamente
          echo "$HOME/.dotnet/tools" >> $GITHUB_PATH
          
          # Verificar instalação com timeout
          timeout 30s sarif --version || {
            echo "❌ Falha na verificação do SARIF MultiTool"
            exit 1
          }
          
          echo "✅ SARIF MultiTool instalado com sucesso"

      # ========== CONVERT FPR TO SARIF ==========
      - name: Convert FPR files to SARIF
        shell: bash
        env:
          FORTIFY_SSC_API_URL: ${{ secrets.FORTIFY_SSC_API_URL }}
          FORTIFY_UNIFIED_LOGIN_TOKEN: ${{ secrets.FORTIFY_UNIFIED_LOGIN_TOKEN }}
          FORTIFY_PROJECT_NAME: ${{ inputs.fortify_project_name }}
          FORTIFY_VERSION_NAME: ${{ inputs.fortify_version_name }}
        run: |
          # Configurações robustas
          set -euo pipefail
          IFS=$'\n\t'
          
          # Trap para cleanup
          trap 'echo "❌ Script falhou na linha $LINENO"' ERR
          
          echo "🔄 Iniciando conversão FPR → SARIF (Linux)..."
          
          # ========== FUNÇÕES UTILITÁRIAS LINUX (baseadas no fortify-sast.yml) ==========
          
          # Função para validação JSON (baseada no fortify-sast.yml)
          validate_json_response() {
              local response="$1"
              local context="$2"
              
              if [ -z "$response" ]; then
                  echo "❌ Resposta vazia da API ($context)"
                  return 1
              fi
              
              if ! echo "$response" | jq -e . >/dev/null 2>&1; then
                  echo "❌ JSON inválido ($context):"
                  echo "$response" | head -10
                  return 1
              fi
              
              local error_msg=$(echo "$response" | jq -r '.message // empty')
              if [ -n "$error_msg" ]; then
                  echo "❌ Erro da API ($context): $error_msg"
                  return 1
              fi
              
              return 0
          }
          
          # Função para chamadas API (baseada no fortify-sast.yml)
          api_call() {
              local method="${1:-GET}"
              local url="$2"
              local data="${3:-}"
              local context="$4"
              
              local curl_opts=(-sS -k -L --fail)
              curl_opts+=(-H "Content-Type: application/json")
              curl_opts+=(-H "Accept: application/json")
              curl_opts+=(-H "Authorization: FortifyToken $FORTIFY_UNIFIED_LOGIN_TOKEN")
              
              if [ "$method" = "POST" ] || [ "$method" = "PUT" ]; then
                  curl_opts+=(-X "$method")
                  if [ -n "$data" ]; then
                      curl_opts+=(-d "$data")
                  fi
              fi
              
              curl_opts+=("$url")
              
              local response
              local exit_code
              
              response=$(curl "${curl_opts[@]}" 2>/dev/null) || exit_code=$?
              
              if [ "${exit_code:-0}" -ne 0 ]; then
                  echo "❌ Falha na chamada API ($context): $exit_code"
                  return 1
              fi
              
              if ! validate_json_response "$response" "$context"; then
                  return 1
              fi
              
              echo "$response"
              return 0
          }
          
          # Função para buscar projeto/versão no Fortify SSC (Linux)
          fortify_buscar_projeto_versao() {
              echo "🔍 Buscando projeto/versão no Fortify SSC (Linux)..."
              
              if [ -n "$FORTIFY_SSC_API_URL" ] && [ -n "$FORTIFY_UNIFIED_LOGIN_TOKEN" ]; then
                  local project_response
                  local encoded_name=$(echo "$FORTIFY_PROJECT_NAME" | jq -rR '@uri')
                  project_response=$(api_call "GET" "$FORTIFY_SSC_API_URL/projectVersions?q=$encoded_name&fulltextsearch=true" "" "buscar projeto")
                  
                  if [ $? -eq 0 ]; then
                      local project_id=$(echo "$project_response" | jq -r '.data[] | select(.project.name=="'"$FORTIFY_PROJECT_NAME"'") | .project.id' | head -1)
                      local version_id=$(echo "$project_response" | jq -r '.data[] | select(.project.name=="'"$FORTIFY_PROJECT_NAME"'") | select(.name=="'"$FORTIFY_VERSION_NAME"'") | .id' | head -1)
                      
                      if [ -n "$version_id" ]; then
                          echo "✅ Projeto e versão encontrados: $version_id"
                          FORTIFY_PROJECT_VERSION_ID="$version_id"
                          return 0
                      elif [ -n "$project_id" ]; then
                          echo "✅ Projeto encontrado, versão não existe"
                          FORTIFY_PROJECT_ID="$project_id"
                          return 1
                      else
                          echo "⚠️ Projeto não encontrado"
                          return 2
                      fi
                  else
                      echo "⚠️ Falha ao buscar projeto no SSC"
                      return 3
                  fi
              else
                  echo "⚠️ Secrets do Fortify não configurados"
                  return 4
              fi
          }
          
          # Criar diretório de saída
          mkdir -p ./sarif-output
          
          # Contador de arquivos
          fpr_count=0
          converted_count=0
          failed_count=0
          
          # Buscar projeto/versão no Fortify SSC
          fortify_buscar_projeto_versao
          fortify_status=$?
          
          # Buscar todos os arquivos .fpr
          shopt -s globstar
          for fpr_file in ./fpr-files/**/*.fpr; do
            if [ -f "$fpr_file" ]; then
              fpr_count=$((fpr_count + 1))
              echo "📁 Processando: $fpr_file"
              
              # Gerar nome do arquivo SARIF
              sarif_file="./sarif-output/$(basename "$fpr_file" .fpr).sarif"
              
              # Converter usando SARIF MultiTool
              if sarif convert "$fpr_file" --tool FortifyFpr --output "$sarif_file" --pretty-print --force; then
                
                converted_count=$((converted_count + 1))
                echo "✅ Convertido: $sarif_file"
                
                # Pós-processamento para melhorar compatibilidade
                if [ -f "$sarif_file" ]; then
                  echo "🔧 Aplicando melhorias de compatibilidade..."
                  
                  # Função melhorada para aplicar metadados SARIF
                  apply_fortify_metadata() {
                      local sarif_file="$1"
                      local temp_file="${sarif_file}.tmp"
                      
                      # Validações de entrada
                      if [[ ! -f "$sarif_file" ]]; then
                          echo "⚠️ Arquivo SARIF não encontrado: $sarif_file"
                          return 1
                      fi
                      
                      if [[ ! -s "$sarif_file" ]]; then
                          echo "⚠️ Arquivo SARIF vazio: $sarif_file"
                          return 1
                      fi
                      
                      # Validar JSON básico
                      if ! jq empty "$sarif_file" 2>/dev/null; then
                          echo "⚠️ Arquivo SARIF com JSON inválido: $sarif_file"
                          return 1
                      fi
                      
                      # Validar estrutura mínima SARIF
                      local schema_version=$(jq -r '."$schema" // empty' "$sarif_file" 2>/dev/null)
                      local sarif_version=$(jq -r '.version // empty' "$sarif_file" 2>/dev/null)
                      
                      if [[ -z "$schema_version" || -z "$sarif_version" ]]; then
                          echo "⚠️ Estrutura SARIF inválida em: $sarif_file"
                          return 1
                      fi
                      
                      local timestamp=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
                      local fortify_version="25.2.0"
                      
                      # Aplicar transformações com validação robusta
                      if jq --arg timestamp "$timestamp" \
                            --arg project_name "$FORTIFY_PROJECT_NAME" \
                            --arg version_name "$FORTIFY_VERSION_NAME" \
                            --arg fortify_version "$fortify_version" \
                            --arg version_id "${FORTIFY_PROJECT_VERSION_ID:-0}" '
                         # Garantir estrutura mínima
                         if (.runs | type) != "array" or (.runs | length) == 0 then 
                             .runs = [{"tool": {"driver": {}}, "invocations": [{}], "results": []}] 
                         else . end |
                         
                         # Garantir estruturas obrigatórias
                         (.runs[0].tool.driver.name = "Fortify Static Code Analyzer") |
                         (.runs[0].tool.driver.version = $fortify_version) |
                         (.runs[0].tool.driver.informationUri = "https://www.microfocus.com/en-us/cyberres/application-security/static-code-analyzer") |
                         
                         # Garantir invocations
                         if (.runs[0].invocations | type) != "array" or (.runs[0].invocations | length) == 0 then
                             .runs[0].invocations = [{}]
                         else . end |
                         
                         (.runs[0].invocations[0].startTimeUtc = $timestamp) |
                         (.runs[0].invocations[0].endTimeUtc = $timestamp) |
                         (.runs[0].invocations[0].executionSuccessful = true) |
                         (.runs[0].invocations[0].commandLine = "scancentral") |
                         (.runs[0].invocations[0].workingDirectory = {"uri": "file:///github/workspace/"}) |
                         
                         # Processar results com validação completa
                         if (.runs[0].results | type) == "array" then
                             .runs[0].results |= map(
                                 if (.locations | type) == "array" and (.locations | length) > 0 and
                                    .locations[0].physicalLocation.artifactLocation.uri and
                                    .locations[0].physicalLocation.region.startLine and
                                    .ruleId then
                                     . + {
                                         "partialFingerprints": {
                                             "primaryLocationLineHash": (.locations[0].physicalLocation.artifactLocation.uri + ":" + (.locations[0].physicalLocation.region.startLine | tostring) + ":" + .ruleId)
                                         },
                                         "properties": {
                                             "fortify_project": $project_name,
                                             "fortify_version": $version_name,
                                             "fortify_version_id": $version_id,
                                             "fortify_scan_time": $timestamp,
                                             "fortify_engine_version": $fortify_version
                                         }
                                     }
                                 else
                                     .
                                 end
                             )
                         else . end' "$sarif_file" > "$temp_file"; then
                          
                          # Validar arquivo gerado
                          if jq empty "$temp_file" 2>/dev/null && [[ -s "$temp_file" ]]; then
                              mv "$temp_file" "$sarif_file"
                              echo "✅ Metadados Fortify aplicados com sucesso"
                              return 0
                          else
                              rm -f "$temp_file"
                              echo "⚠️ Arquivo SARIF gerado inválido"
                              return 1
                          fi
                      else
                          rm -f "$temp_file"
                          echo "⚠️ Falha na transformação JQ"
                          return 1
                      fi
                  }
                  
                  if apply_fortify_metadata "$sarif_file"; then
                    echo "✅ Metadados Fortify adicionados"
                  else
                    echo "⚠️ Falha ao adicionar metadados Fortify"
                  fi
                fi
                
                # Verificar tamanho do arquivo
                if [ -f "$sarif_file" ]; then
                  file_size=$(wc -c < "$sarif_file")
                  echo "📊 Tamanho: $((file_size/1024))KB"
                fi
              else
                failed_count=$((failed_count + 1))
                echo "❌ Falha na conversão: $fpr_file"
              fi
            fi
          done
          
          # Verificar se há arquivos FPR para processar
          if [ "$fpr_count" -eq 0 ]; then
            echo "❌ Nenhum arquivo FPR encontrado"
            exit 1
          fi
          
          echo "📈 Resumo da conversão (Linux):"
          echo "   • Arquivos FPR encontrados: $fpr_count"
          echo "   • Arquivos convertidos: $converted_count"
          echo "   • Falhas na conversão: $failed_count"
          
          # Calcular taxa de sucesso
          if [ "$fpr_count" -gt 0 ]; then
            success_rate=$((converted_count * 100 / fpr_count))
            echo "   • Taxa de sucesso: ${success_rate}%"
          else
            echo "   • Taxa de sucesso: N/A (nenhum arquivo FPR encontrado)"
          fi
          
          # Falhar se não houve conversões bem-sucedidas
          if [ "$converted_count" -eq 0 ] && [ "$fpr_count" -gt 0 ]; then
            echo "❌ Nenhum arquivo foi convertido com sucesso"
            exit 1
          fi
          
          # Gerar resumo para GitHub
          echo "### 📊 Conversão FPR → SARIF (Linux)" >> $GITHUB_STEP_SUMMARY
          echo "| Métrica | Valor | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| FPR Processados | $fpr_count | ✅ |" >> $GITHUB_STEP_SUMMARY
          echo "| SARIF Gerados | $converted_count | $([ $converted_count -gt 0 ] && echo "✅" || echo "❌") |" >> $GITHUB_STEP_SUMMARY
          echo "| Taxa Sucesso | ${success_rate}% | $([ $success_rate -gt 90 ] && echo "✅" || echo "⚠️") |" >> $GITHUB_STEP_SUMMARY
          echo "| Tempo Execução | $(date -u -d @$SECONDS +%M:%S) | ✅ |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔍 Status da Integração SSC" >> $GITHUB_STEP_SUMMARY
          echo "| Componente | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| API Endpoint | ${FORTIFY_SSC_API_URL:+✅ Configurado} ${FORTIFY_SSC_API_URL:-❌ Não configurado} |" >> $GITHUB_STEP_SUMMARY
          echo "| Token Auth | ${FORTIFY_UNIFIED_LOGIN_TOKEN:+✅ Configurado} ${FORTIFY_UNIFIED_LOGIN_TOKEN:-❌ Não configurado} |" >> $GITHUB_STEP_SUMMARY
          echo "| Projeto/Versão | ${FORTIFY_PROJECT_VERSION_ID:+✅ Encontrado ($FORTIFY_PROJECT_VERSION_ID)} ${FORTIFY_PROJECT_VERSION_ID:-⚠️ Não encontrado} |" >> $GITHUB_STEP_SUMMARY

      # ========== VALIDATE SARIF FILES ==========
      - name: Validate SARIF files
        if: ${{ fromJSON(inputs.validate_sarif) }}
        shell: bash
        run: |
          echo "🔍 Validando arquivos SARIF..."
          
          validation_errors=0
          valid_files=0
          total_files=0
          
          for sarif_file in ./sarif-output/*.sarif; do
            if [ -f "$sarif_file" ]; then
              total_files=$((total_files + 1))
              echo "🔍 Validando: $(basename "$sarif_file")"
              
              if sarif validate "$sarif_file"; then
                valid_files=$((valid_files + 1))
                echo "✅ Válido: $(basename "$sarif_file")"
              else
                validation_errors=$((validation_errors + 1))
                echo "❌ Inválido: $(basename "$sarif_file")"
              fi
            fi
          done
          
          echo "📊 Resultado da validação:"
          echo "   • Total de arquivos: $total_files"
          echo "   • Arquivos válidos: $valid_files"
          echo "   • Erros de validação: $validation_errors"
          
          if [ $validation_errors -gt 0 ]; then
            echo "⚠️ Alguns arquivos SARIF falharam na validação"
          fi
          
          # Falhar se todos os arquivos são inválidos
          if [ "$valid_files" -eq 0 ] && [ "$total_files" -gt 0 ]; then
            echo "❌ Todos os arquivos SARIF são inválidos"
            exit 1
          fi

      # ========== ANALYZE SARIF CONTENT ==========
      - name: Analyze SARIF content
        shell: bash
        run: |
          echo "📊 Análise detalhada dos arquivos SARIF..."
          
          for sarif_file in ./sarif-output/*.sarif; do
            if [ -f "$sarif_file" ]; then
              echo ""
              echo "📋 $(basename "$sarif_file"):"
              
              # Contar resultados por nível
              critical_high=$(jq '.runs[0].results[] | select(.level == "error") | .ruleId' "$sarif_file" 2>/dev/null | wc -l)
              medium=$(jq '.runs[0].results[] | select(.level == "warning") | .ruleId' "$sarif_file" 2>/dev/null | wc -l)
              low=$(jq '.runs[0].results[] | select(.level == "note") | .ruleId' "$sarif_file" 2>/dev/null | wc -l)
              total=$(jq '.runs[0].results | length' "$sarif_file" 2>/dev/null || echo "0")
              
              echo "   • Total de findings: $total"
              echo "   • Critical/High: $critical_high"
              echo "   • Medium: $medium"
              echo "   • Low: $low"
              
              # Verificar regras únicas
              unique_rules=$(jq '.runs[0].results[].ruleId' "$sarif_file" 2>/dev/null | sort | uniq | wc -l)
              echo "   • Regras únicas: $unique_rules"
            fi
          done

      # ========== UPLOAD SARIF ARTIFACTS ==========
      - name: Upload SARIF artifacts
        uses: actions/upload-artifact@v4
        with:
          name: fortify-sarif-linux
          path: './sarif-output/*.sarif'
          if-no-files-found: error 
