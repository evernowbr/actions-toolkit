name: Fortify FPR to SARIF Converter

on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string
      version:
        required: true
        type: string
      dotnet_version:
        description: 'Versão do .NET SDK'
        required: false
        default: '8.0.x'
        type: string
      validate_sarif:
        description: 'Validar SARIF gerado'
        required: false
        default: 'true'
        type: string
    secrets:
      FORTIFY_SSC_API_URL:
        required: true
      FORTIFY_UNIFIED_LOGIN_TOKEN:
        required: true

jobs:
  convert:
    name: Fortify FPR to SARIF Conversion
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y jq curl unzip

      - name: Verify and Download FPR
        env:
          FORTIFY_PROJECT_NAME: ${{ inputs.name }}
          FORTIFY_VERSION_NAME: ${{ inputs.version }}
          FORTIFY_SSC_API_URL: ${{ secrets.FORTIFY_SSC_API_URL }}
          FORTIFY_UNIFIED_LOGIN_TOKEN: ${{ secrets.FORTIFY_UNIFIED_LOGIN_TOKEN }}
        run: |
          set -euo pipefail
          
          echo "📥 Verificando e baixando FPR existente..."
          
          # Função para chamadas API
          api_call() {
              local method="${1:-GET}"
              local url="$2"
              local context="$3"
              
              curl -k -sSL --fail \
                  -H "Content-Type: application/json" \
                  -H "Accept: application/json" \
                  -H "Authorization: FortifyToken $FORTIFY_UNIFIED_LOGIN_TOKEN" \
                  ${method:+-X "$method"} \
                  "$url"
          }
          
          # Buscar projeto/versão
          echo "🔍 Buscando projeto: $FORTIFY_PROJECT_NAME/$FORTIFY_VERSION_NAME"
          project_response=$(api_call "GET" "$FORTIFY_SSC_API_URL/api/v1/projectVersions?q=$FORTIFY_PROJECT_NAME&fulltextsearch=true" "verificar projeto")
          
          version_id=$(echo "$project_response" | jq -r --arg proj "$FORTIFY_PROJECT_NAME" --arg ver "$FORTIFY_VERSION_NAME" \
              '.data[] | select(.project.name==$proj and .name==$ver) | .id')
          
          if [ -z "$version_id" ] || [ "$version_id" == "null" ]; then
              echo "❌ Projeto/versão não encontrado: $FORTIFY_PROJECT_NAME/$FORTIFY_VERSION_NAME"
              echo "💡 Certifique-se de que o scan já foi executado"
              exit 1
          fi
          
          echo "✅ Projeto encontrado: $version_id"
          echo "FORTIFY_PROJECT_VERSION_ID=$version_id" >> $GITHUB_ENV
          
          # Buscar FPR mais recente
          echo "📋 Buscando artefatos FPR..."
          artifacts_response=$(api_call "GET" "$FORTIFY_SSC_API_URL/api/v1/projectVersions/$version_id/artifacts" "buscar artefatos")
          
          # Pegar o FPR mais recente (ordenado por data)
          fpr_id=$(echo "$artifacts_response" | jq -r '.data[] | select(.name | endswith(".fpr")) | .id' | head -1)
          
          if [ -z "$fpr_id" ] || [ "$fpr_id" == "null" ]; then
              echo "❌ Nenhum FPR encontrado para este projeto/versão"
              echo "💡 Execute um scan Fortify antes de converter para SARIF"
              exit 1
          fi
          
          echo "✅ FPR encontrado: $fpr_id"
          
          # Criar diretório e baixar FPR
          mkdir -p fortify-artifacts
          fpr_filename="$(echo "$FORTIFY_PROJECT_NAME" | sed 's/\//-/g')-$FORTIFY_VERSION_NAME.fpr"
          
          echo "📥 Baixando FPR: $fpr_filename"
          if curl -k -sSL \
              -H "Authorization: FortifyToken $FORTIFY_UNIFIED_LOGIN_TOKEN" \
              -H "Accept: application/octet-stream" \
              "$FORTIFY_SSC_API_URL/api/v1/projectVersions/$version_id/artifacts/$fpr_id/content" \
              -o "fortify-artifacts/$fpr_filename"; then
              
              file_size=$(wc -c < "fortify-artifacts/$fpr_filename")
              echo "✅ FPR baixado com sucesso: $fpr_filename ($((file_size/1024/1024))MB)"
              echo "FPR_FILENAME=$fpr_filename" >> $GITHUB_ENV
          else
              echo "❌ Falha ao baixar FPR"
              exit 1
          fi

      - name: Validate FPR File
        run: |
          echo "🔍 Validando arquivo FPR..."
          
          fpr_file="fortify-artifacts/$FPR_FILENAME"
          
          # Verificar se arquivo existe e não está vazio
          if [ ! -f "$fpr_file" ]; then
              echo "❌ Arquivo FPR não encontrado: $fpr_file"
              exit 1
          fi
          
          file_size=$(wc -c < "$fpr_file")
          if [ "$file_size" -lt 1024 ]; then
              echo "❌ Arquivo FPR muito pequeno ($file_size bytes) - pode estar corrompido"
              exit 1
          fi
          
          # Verificar cabeçalho FPR (arquivos FPR começam com bytes específicos)
          if ! file "$fpr_file" | grep -q "data"; then
              echo "❌ Arquivo não parece ser um FPR válido"
              file "$fpr_file"
              exit 1
          fi
          
          echo "✅ FPR validado: $((file_size/1024/1024))MB"
          echo "📊 Arquivo pronto para conversão"

      - name: Setup .NET SDK for SARIF Conversion
        uses: actions/setup-dotnet@6bd8b7f7774af54e05809fcc5431931b3eb1ddee
        with:
          dotnet-version: ${{ inputs.dotnet_version }}

      - name: Install Latest Compatible SARIF MultiTool
        run: |
          echo "🔧 Preparando runner para conversão SARIF..."
          
          # Verificar versões disponíveis do SARIF MultiTool
          echo "📋 Verificando versões SARIF MultiTool disponíveis..."
          available_versions=$(dotnet tool search Sarif.Multitool | grep "Sarif.Multitool" | head -5)
          echo "$available_versions"
          
          # Tentar instalar versões em ordem de preferência (mais recente primeiro)
          versions_to_try=("4.8.0" "4.7.1" "4.7.0" "4.6.0")
          installed=false
          
          for version in "${versions_to_try[@]}"; do
              echo "🔄 Tentando instalar SARIF MultiTool v$version..."
              
              if dotnet tool install --global Sarif.Multitool --version "$version" 2>/dev/null; then
                  echo "✅ SARIF MultiTool v$version instalado com sucesso"
                  echo "SARIF_VERSION=$version" >> $GITHUB_ENV
                  installed=true
                  break
              else
                  echo "⚠️ Versão $version não disponível, tentando próxima..."
              fi
          done
          
          if [ "$installed" = false ]; then
              echo "❌ Nenhuma versão compatível encontrada"
              echo "💡 Tentando instalar a versão mais recente disponível..."
              if dotnet tool install --global Sarif.Multitool; then
                  echo "✅ SARIF MultiTool (latest) instalado"
              else
                  echo "❌ Falha total na instalação do SARIF MultiTool"
                  exit 1
              fi
          fi
          
          # Adicionar ao PATH e verificar
          echo "$HOME/.dotnet/tools" >> $GITHUB_PATH
          export PATH="$HOME/.dotnet/tools:$PATH"
          
          # Testar se funciona
          if sarif --version >/dev/null 2>&1; then
              echo "✅ SARIF MultiTool pronto para conversão"
              sarif --version
          else
              echo "❌ SARIF MultiTool instalado mas não funciona"
              exit 1
          fi

      - name: Convert FPR to SARIF
        env:
          FORTIFY_PROJECT_NAME: ${{ inputs.name }}
          FORTIFY_VERSION_NAME: ${{ inputs.version }}
        run: |
          echo "🔄 Convertendo FPR → SARIF com runner otimizado..."
          
          fpr_file="fortify-artifacts/$FPR_FILENAME"
          sarif_file="fortify-artifacts/fortify-sast-results.sarif"
          
          # Conversão com retry
          conversion_success=false
          for attempt in {1..3}; do
              echo "📁 Tentativa $attempt: Convertendo $(basename "$fpr_file")"
              
              if sarif convert "$fpr_file" \
                    --tool FortifyFpr \
                    --output "$sarif_file" \
                    --pretty-print --force; then
                  
                  echo "✅ Conversão bem-sucedida na tentativa $attempt"
                  conversion_success=true
                  break
              else
                  echo "⚠️ Tentativa $attempt falhou"
                  if [ $attempt -lt 3 ]; then
                      sleep 5
                  fi
              fi
          done
          
          if [ "$conversion_success" = false ]; then
              echo "❌ Falha na conversão após 3 tentativas"
              exit 1
          fi
          
          # Verificar se SARIF foi gerado
          if [ ! -f "$sarif_file" ]; then
              echo "❌ Arquivo SARIF não foi gerado"
              exit 1
          fi
          
          # Adicionar metadados Fortify
          echo "🏷️ Adicionando metadados Fortify..."
          timestamp=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          
          jq --arg project "$FORTIFY_PROJECT_NAME" \
             --arg version "$FORTIFY_VERSION_NAME" \
             --arg timestamp "$timestamp" \
             --arg version_id "${FORTIFY_PROJECT_VERSION_ID:-unknown}" \
             '(.runs[0].properties // {}) |= . + {
                "fortify_project": $project,
                "fortify_version": $version,
                "fortify_version_id": $version_id,
                "fortify_scan_time": $timestamp,
                "fortify_engine_version": "25.2.0",
                "conversion_method": "existing_fpr_direct"
              }' "$sarif_file" > "${sarif_file}.tmp" && mv "${sarif_file}.tmp" "$sarif_file"
          
          # Estatísticas
          file_size=$(wc -c < "$sarif_file")
          findings=$(jq '.runs[0].results | length' "$sarif_file" 2>/dev/null || echo "0")
          rules=$(jq '.runs[0].tool.driver.rules | length' "$sarif_file" 2>/dev/null || echo "0")
          
          echo "📊 SARIF gerado com sucesso:"
          echo "   • Tamanho: $((file_size/1024))KB"
          echo "   • Findings: $findings"
          echo "   • Regras: $rules"

      - name: Validate SARIF Output
        if: ${{ fromJSON(inputs.validate_sarif) }}
        run: |
          echo "🔍 Validação final do SARIF..."
          
          sarif_file="fortify-artifacts/fortify-sast-results.sarif"
          
          if [ -f "$sarif_file" ]; then
              if sarif validate "$sarif_file" 2>/dev/null; then
                  echo "✅ SARIF válido e pronto para uso"
              else
                  echo "❌ SARIF gerado mas inválido"
                  echo "🔍 Tentando identificar o problema..."
                  sarif validate "$sarif_file" || true
                  exit 1
              fi
          else
              echo "❌ Arquivo SARIF não encontrado"
              exit 1
          fi

      - name: Upload SARIF Artifact
        uses: actions/upload-artifact@50769540e7f4bd5e21e526ee35c689e35e0d6874
        with:
          name: fortify-sarif-optimized-${{ github.run_number }}
          path: fortify-artifacts/fortify-sast-results.sarif
          if-no-files-found: error
          retention-days: 30 
